I want to build a Golang tool. So this tool will be a net binary, and it's purposeful to gather network info on a, from a machine. What I would like it to do, it needs to determine all of the interfaces, it needs to determine the routes, it needs to determine the public IP, obviously the current IP, and any other pertinent network information I can't think about, such as probably DNS information at this point, DNS resolution info would probably be helpful. Basically, I wanna turn this into a tool, CLI tool that we just wanna call, you know, like the name of it, like net data or something. I'm not sure what the name could be. Maybe net stats, net data. Yeah, I'd say net data, and it'll output all of this information that we can then look at. Like I said, I want it written in Golang, and I wanna try to have it minimal as possible to not use a lot of anything outside the standard lib, although I do understand that we're gonna have to leverage other dependencies more than likely.